# خطوات رفع البوت على Replit

## الخطوة 1: إنشاء Repl جديد

1. سجل في Replit: https://replit.com
2. اضغط "Create Repl" (أو "+" في الأعلى)
3. اختر "Python"
4. اكتب اسم: `karys-shop-bot`
5. اضغط "Create Repl"

## الخطوة 2: رفع الملفات

1. في Replit، اضغط على أيقونة الملفات (📁) في اليسار
2. ارفع هذه الملفات:
   - `bot.py`
   - `requirements.txt`
   - `keep_alive.py`

**كيفية الرفع:**
- اضغط على أيقونة الملفات
- اضغط "Upload file"
- اختر الملفات

## الخطوة 3: إضافة Token

1. اضغط على أيقونة "Secrets" (🔒) في اليسار
2. اضغط "New secret"
3. Name: `DISCORD_BOT_TOKEN`
4. Value: الصق token البوت
5. اضغط "Add secret"

## الخطوة 4: تعديل bot.py

1. افتح `bot.py` في Replit
2. أزل التعليق من هذه الأسطر (احذف `#`):

**في أول الملف (بعد imports):**
```python
from keep_alive import keep_alive
```

**قبل `bot.run(token)`:**
```python
keep_alive()
```

## الخطوة 5: تشغيل البوت

1. اضغط "Run" (أو Ctrl+Enter)
2. انتظر حتى ترى: `Bot logged in as: KARYS SHOP#2111`
3. البوت يعمل الآن!

## ملاحظات مهمة:

- ✅ البوت سيعمل 24/7 طالما Replit مفتوح
- ✅ لا تغلق نافذة Replit
- ✅ إذا توقف البوت، اضغط "Run" مرة أخرى
- ⚠️ Replit المجاني قد يوقف البوت بعد 1 ساعة من عدم الاستخدام

## حل المشاكل:

**البوت لا يعمل:**
- تحقق من Token في Secrets
- تأكد من رفع جميع الملفات
- تحقق من الـ logs في Console

**البوت يتوقف:**
- اضغط "Run" مرة أخرى
- أو استخدم keep_alive.py (موجود في الملفات)
